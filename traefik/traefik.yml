api:
  dashboard: true
  insecure: true   # ✅ this exposes the dashboard on port 8080

entryPoints:
  web:
    address: ":80"

  traefik:
    address: ":8080"   # ✅ dashboard listens here

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

log:
  level: INFO
